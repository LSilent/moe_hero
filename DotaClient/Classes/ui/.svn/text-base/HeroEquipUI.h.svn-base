#ifndef __HEROEQUIPUI_H__
#define __HEROEQUIPUI_H__
#include "./ui/ui.h"

class HeroEquipUI :public UI
{
public:
	HeroEquipUI();
	virtual ~HeroEquipUI();

protected:

	virtual   X_VOID   CareEvent();
	virtual	  X_VOID   OnGameEvent(const tGameEventData* pEvent);
	virtual   X_VOID   OnCreate();

	X_VOID				UpData();	
};
#endif